# Tokenized Market Simulator 프로젝트 구조 분석

## 프로젝트 한 줄 요약

이 프로젝트는 **삼성전자 기준 가격을 따라가는 모의 ERC-20 토큰 `mSEC`를 모의 원화 토큰 `mKRW`로 사고파는 Android 기반 모의 현물 거래소**이다.

실제 삼성전자 주식이나 투자상품을 토큰화하는 것은 아니며, 블록체인·백엔드·Android·실시간 통신을 종합적으로 학습하고 졸업작품 및 포트폴리오로 보여주기 위한 프로젝트다.

이 문서는 저장소의 문서, 설정, Solidity, Java, Kotlin 소스를 바탕으로 프로젝트 구조와 현재 구현 상태를 정리한다. 바이너리인 Gradle Wrapper JAR와 IDE 메타데이터는 구조와 용도를 기준으로 확인했으며, HWP 제안서는 내부 본문 스트림을 추출해 검토했다.

---

## 전체 구조

```text
Android 앱
   │ REST / WebSocket
   ▼
Spring Boot 백엔드 ─── PostgreSQL
   │ web3j / RPC
   ▼
Ethereum 로컬체인(Anvil)
   │
   ├─ mKRW
   ├─ mSEC
   ├─ PriceOracle
   └─ ExchangeVault
```

---

## 디렉터리별 설명

### 루트

프로젝트 전체를 묶는 모노레포 최상위다.

- `README.md`: 프로젝트 요약, 구조, 실행 순서
- `구현 계획.md`: 가장 중요한 설계 문서이자 현재의 Source of Truth
- `docker-compose.yml`: PostgreSQL 16과 로컬 Ethereum 체인 Anvil 실행
- `졸업프로젝트 제안서_김성은, 서교빈.hwp`: 최초 졸업작품 제안서
- `.gitignore`: 환경 파일, IDE 파일, HWP 등을 Git에서 제외

### `claude-docs/`

이 디렉터리는 애플리케이션 실행에 사용되지 않는다. Claude가 이전 세션에서 이해한 프로젝트 정보와 구현 현황을 저장해 두는 **공유 메모리** 역할을 한다.

- `README.md`: 이 디렉터리의 목적
- `project-overview.md`: 프로젝트와 기술 스택의 짧은 요약
- `implementation-log.md`: 실제 구현된 부분, 미구현 부분, 검증 결과와 당시 환경

문서의 우선순위는 다음과 같이 보는 것이 좋다.

```text
구현 계획.md
    ↓ 가장 신뢰할 설계 기준
실제 소스 코드
    ↓ 현재 구현 상태
claude-docs
    ↓ 요약과 작업 기록
README
```

`claude-docs`는 빠르게 맥락을 파악하는 데 유용하지만 자동으로 최신화되는 문서는 아니다. 실제 코드와 충돌하면 코드 및 `구현 계획.md`를 우선해야 한다.

### `contracts/`

Solidity와 Foundry로 작성된 온체인 정산 영역이다. 현재 가장 완성도가 높은 모듈이다.

- `MockKRW.sol`: 테스트용 모의 원화 `mKRW`
  - 누구나 `faucet()`으로 1,000,000 mKRW 발행 가능
- `SamsungPriceTrackingToken.sol`: 삼성전자 기준 가격 추종 토큰 `mSEC`
  - 지정된 `ExchangeVault`만 발행·소각 가능
- `PriceOracle.sol`: 현재 기준 가격 저장
  - 가격은 `1e8` 정밀도
  - 소유자만 가격 변경 가능
- `ExchangeVault.sol`: mKRW와 mSEC 간 매수·매도 정산
  - 기본 수수료 0.1%
  - 매수 시 mKRW 수취 후 mSEC 발행
  - 매도 시 mSEC 소각 후 mKRW 지급
  - 재진입 방지와 안전한 ERC-20 전송 사용

그 밖의 주요 파일은 다음과 같다.

- `script/Deploy.s.sol`: 컨트랙트 4개 배포 및 권한 연결
- `script/Scenario.s.sol`: 75,000원 매수 → 80,000원 가격 변경 → 전량 매도 시나리오
- `test/Exchange.t.sol`: faucet, 가격 변경, 매수·매도, 유동성 부족 등 6개 테스트
- `foundry.toml`: Solidity 0.8.24, 최적화, 라이브러리 및 RPC 설정

이 모듈은 단순 골격이 아니라 실제 가격 추종 매수·매도 계산이 구현되어 있다.

### `backend/`

Java 21과 Spring Boot 3.3 기반 서버다. 설계 골격과 일부 기능만 구현된 상태다.

패키지별 역할은 다음과 같다.

- `auth`: 회원가입, 로그인, JWT
- `user`: 사용자 JPA 엔티티와 저장소
- `wallet`: 사용자 블록체인 지갑 관리
- `market`: 현재가 API 및 모의 가격 시뮬레이터
- `quote`: 매수·매도 예상 견적
- `order`: 주문 생성 및 상태 관리
- `trade`: 체결 내역
- `portfolio`: 자산 평가와 손익
- `blockchain`: web3j를 이용한 Ethereum 연결
- `websocket`: STOMP/WebSocket 설정
- `common`: 보안 설정과 상태 확인

현재 실제로 동작하는 부분은 다음과 같다.

- `GET /api/health`
- `GET /api/markets`
- `GET /api/markets/{symbol}`
- 75,000원부터 ±0.3%씩 변하는 가격 시뮬레이터
- `/topic/markets/mSEC/price` 가격 이벤트 발행
- 매수·매도 견적 계산
- JWT 생성과 토큰 사용자 ID 추출
- WebSocket/STOMP 설정
- 사용자 JPA 엔티티
- web3j 최신 블록 번호 조회

아직 구현되지 않은 부분은 다음과 같다.

- 실제 회원가입과 로그인
- JWT 인증 필터 연결
- 가격 이력 DB 저장
- 주문 생성과 조회
- 포트폴리오 계산
- 지갑 생성 또는 연결
- 컨트랙트 `buy`/`sell` 호출
- 트랜잭션 receipt 추적
- 체결 저장 및 사용자별 이벤트 발행

DB 스키마에는 다음 6개 테이블이 계획되어 있다.

```text
users
assets
price_ticks
orders
trades
blockchain_transactions
```

즉, 백엔드는 현재 **실행 가능한 스캐폴딩과 가격·견적 데모** 수준이다.

### `android/`

Kotlin과 Jetpack Compose 기반 모바일 클라이언트다. 화면과 레이어 구조는 마련됐지만 실제 서비스 연결은 대부분 미완성이다.

구조는 클린 아키텍처 형태다.

```text
data          REST/WebSocket 구현
domain        모델, Repository 인터페이스, UseCase
presentation  Compose 화면과 내비게이션
```

현재 준비된 화면은 다음과 같다.

- 로그인
- 회원가입
- 마켓
- 거래
- 포트폴리오
- 주문/체결 내역
- 마이페이지
- 하단 탭 내비게이션

구현된 부분은 다음과 같다.

- Compose 앱 진입점과 테마
- 화면 이동
- Retrofit API 인터페이스
- Android 에뮬레이터용 백엔드 주소 `10.0.2.2:8080`
- 도메인 모델과 Repository/UseCase 구조
- 일부 인증·마켓 DTO 매핑

미구현 부분은 다음과 같다.

- 로그인 버튼의 실제 API 호출
- ViewModel과 StateFlow
- 실시간 가격 표시
- 매수·매도 입력 및 주문
- 포트폴리오 표시
- 주문 내역 표시
- JWT 저장과 HTTP 인증 헤더
- WebSocket/STOMP 실제 연결
- 주문·포트폴리오 Repository 구현

따라서 지금 앱을 실행하면 화면과 탭은 볼 수 있지만 실제 거래 앱처럼 작동하지는 않는다.

### `.idea/`

IntelliJ 또는 Android Studio의 로컬 프로젝트 설정이다.

- 프로젝트 모듈 정보
- Git 연결 정보
- IDE workspace 상태
- Copilot 마이그레이션 상태

프로젝트 비즈니스 로직과는 무관하며 일반적으로 Git에서 제외해도 되는 로컬 메타데이터다. 현재 루트 `.gitignore`에는 `.idea/` 제외 규칙이 있다.

### `.git/`

Git 커밋, 브랜치, 객체 및 저장소 메타데이터다. 애플리케이션 코드가 아니며 일반적인 기능 구현에서 수정할 대상도 아니다.

---

## HWP 최초 제안서와 현재 프로젝트의 차이

HWP의 초기 제안은 현재 프로젝트보다 훨씬 큰 범위였다.

### 초기 제안

- 테슬라 가격 추종 토큰
- 현물 지정가 주문과 주문 매칭 엔진
- 레버리지 선물 롱/숏
- 증거금과 마진콜
- 자동 강제청산
- Redis 분산 락
- Kafka 비동기 이벤트
- 오라클 주기와 청산 정확도에 관한 논문 실험

### 현재 코드와 구현 계획

- 삼성전자 기준 가격 추종 토큰
- 주문장 없는 오라클 가격 기반 즉시 현물 매수·매도
- PostgreSQL, Spring Boot, WebSocket, Android
- 선물·레버리지·청산은 향후 확장으로 이동

즉, 초기 제안이 상당히 큰 파생상품 플랫폼이었다면 현재 저장소는 먼저 성공 가능한 현물 MVP로 범위를 줄인 상태다. 프로젝트가 기획 단계에서 현실적인 구현 범위로 재정의된 흔적으로 볼 수 있다.

---

## 현재 완성도 판단

현재 단계는 문서에 적힌 것처럼 대략 **Phase 0.5 완료** 상태다.

```text
컨트랙트          실제 동작 및 테스트 완료
백엔드 기반       실행 가능한 골격, 가격·견적 일부 동작
DB               스키마만 준비
WebSocket 서버    가격 발행 골격 동작
온체인 연동       연결 골격만 존재
Android           화면과 레이어 골격만 존재
전체 통합 거래    아직 불가능
```

스마트 컨트랙트에서 핵심 아이디어의 기술적 타당성은 검증됐지만, Android에서 주문하고 백엔드가 컨트랙트를 호출하여 체결 결과를 다시 앱에 전달하는 전체 통합 흐름은 아직 구현되지 않았다.

---

## 권장 개발 순서

1. 백엔드 회원가입·로그인·JWT 완성
2. 주문·체결·가격 이력 엔티티와 Repository 구현
3. DB 기반 mock 거래 API 완성
4. web3j로 컨트랙트 호출 및 트랜잭션 상태 추적
5. 체결·포트폴리오 WebSocket 이벤트 구현
6. Android ViewModel과 Repository를 실제 API에 연결
7. Android에서 전체 매수·매도 시나리오 검증

---

## 연동 전에 확인할 부분

- Android는 금액 필드를 JSON 문자열로 기대하지만 백엔드는 일부 응답에서 `BigDecimal`을 JSON 숫자로 반환한다. 양쪽의 API 계약을 통일해야 한다.
- 서버는 SockJS를 포함한 STOMP 엔드포인트를 제공하지만 Android 클라이언트는 아직 실제 통신 방식이 정해지지 않은 스텁이다. Android에서 사용할 STOMP/WebSocket 라이브러리와 연결 방식을 확정해야 한다.
- 현재 Spring Security는 대부분의 요청을 `permitAll()`로 허용한다. JWT 필터를 연결한 뒤 공개 API와 인증 필요 API를 구분해야 한다.
- 백엔드 가격 시뮬레이터의 가격과 온체인 `PriceOracle` 가격은 아직 자동으로 동기화되지 않는다.
- 견적은 백엔드 가격을 사용하고 실제 거래는 향후 컨트랙트 실행 시점 가격을 사용할 예정이므로, 가격 차이와 슬리피지 정책을 명확히 해야 한다.

---

## 최종 평가

전체적으로 디렉터리와 책임 분리가 명확하고, 설계 문서도 상당히 상세하다. 특히 스마트 컨트랙트는 단순 예제가 아니라 가격 변화가 정산 결과에 반영되는 매수·매도 흐름까지 구현하고 테스트했다.

다만 현재 상태는 완성된 거래소라기보다 **검증된 스마트 컨트랙트 위에 백엔드와 Android의 전체 시스템 골격을 세운 단계**라고 보는 것이 정확하다. 다음 핵심 목표는 백엔드의 DB 기반 거래 흐름을 먼저 완성하고, 그 위에 온체인 호출과 Android UI를 순차적으로 연결하는 것이다.
