# 코드 구조 및 역할

현재 코드는 크게 `contracts`와 `backend`로 나뉜다.

```text
contracts
├─ src       실제 블록체인 프로그램
├─ script    배포·시연 자동화
├─ test      자동 테스트
└─ 설정      Foundry 빌드 설정

backend
├─ java      Spring Boot 서버 코드
├─ resources DB·환경 설정
├─ test      백엔드 테스트
└─ Gradle    빌드·의존성 설정
```

---

# 1. Solidity 컨트랙트

## `contracts/src/`

실제로 블록체인에 배포되는 프로그램이다.

## `MockKRW.sol`

역할:

> 거래에 사용할 테스트용 모의 원화 `mKRW`

핵심 기능:

### `faucet()`

- 호출자에게 기본 1,000,000 mKRW 발행
- 테스트 자금으로 사용
- 실제 원화와 무관

### `setFaucetAmount(amount)`

- 관리자가 faucet 지급량 변경
- `onlyOwner`이므로 배포자만 호출 가능

ERC-20을 상속하므로 다음 기능도 자동으로 가진다.

```text
balanceOf     잔고 조회
transfer      토큰 전송
approve       다른 주소에 사용 권한 부여
allowance     승인 금액 조회
transferFrom  승인받은 토큰 이동
totalSupply   전체 발행량 조회
```

## `SamsungPriceTrackingToken.sol`

역할:

> 사용자가 사고파는 삼성전자 가격 추종 모의 토큰 `mSEC`

핵심 기능:

### `setMinter(address)`

- mSEC를 발행하고 소각할 주소 지정
- 현재는 `ExchangeVault`를 minter로 지정
- 0 주소는 지정할 수 없음
- 소유자만 호출 가능

### `mint(to, amount)`

- 매수 시 사용자에게 mSEC 신규 발행
- 등록된 minter만 호출 가능

### `burn(from, amount)`

- 매도 시 사용자의 mSEC 소각
- 등록된 minter만 호출 가능

핵심 제약:

```text
일반 사용자     mint/burn 불가
관리자          직접 mint/burn 불가
ExchangeVault   mint/burn 가능
```

mSEC에는 가격이 들어 있지 않다. 이 컨트랙트는 누가 몇 개를 보유하는지만 관리한다.

## `PriceOracle.sol`

역할:

> 현재 mSEC의 기준 가격을 블록체인에 저장

핵심 상태:

### `priceE8`

현재 가격이다. 소수점 처리를 위해 `1e8` 배율로 저장한다.

```text
75,000원 → 75,000 × 100,000,000
```

### `updatedAt`

가격을 마지막으로 갱신한 블록 시간이다.

핵심 기능:

### `updatePrice(newPriceE8)`

- 오라클 가격 변경
- 소유자만 호출 가능
- 0원 가격은 거부

### `getPrice()`

- 현재 가격과 마지막 갱신 시각 반환

Phase 3에서는 백엔드가 시뮬레이션 가격을 이 컨트랙트에 전송하는 구조로 확장할 예정이다.

## `ExchangeVault.sol`

역할:

> mKRW와 mSEC를 실제로 교환하는 온체인 정산소

이 프로젝트에서 가장 핵심적인 컨트랙트다.

연결 대상:

```text
krw      MockKRW
token    mSEC
oracle   PriceOracle
```

이 주소들은 `immutable`이므로 배포 후 변경할 수 없다.

### `quoteBuy(krwAmount)`

매수 예상 결과를 조회한다.

```text
입력: 사용할 mKRW
출력: 받을 mSEC, 수수료
```

상태를 변경하지 않는 조회 함수다.

### `buy(krwAmount)`

실제 매수를 수행한다.

```text
사용자 mKRW
→ Vault로 이동

Vault
→ 사용자에게 mSEC 발행
```

호출 전 사용자는 다음 승인을 해야 한다.

```solidity
mKRW.approve(vault, amount)
```

### `quoteSell(tokenAmount)`

매도 예상 결과를 조회한다.

```text
입력: 판매할 mSEC
출력: 받을 mKRW, 수수료
```

### `sell(tokenAmount)`

실제 매도를 수행한다.

```text
사용자 mSEC
→ 소각

Vault mKRW
→ 사용자에게 지급
```

Vault에 지급할 mKRW가 부족하면 거래가 실패한다.

### `setFeeBps(newFeeBps)`

관리자가 수수료를 변경한다.

```text
기본값: 10 bps = 0.1%
최댓값: 1,000 bps = 10%
```

### 보안 기능

- `nonReentrant`: 재진입 공격 방지
- `SafeERC20`: 안전한 ERC-20 전송
- 0 주소 생성자 입력 거부
- 0원 매수·매도 거부
- 관리자 권한 검사
- Vault 유동성 검사

---

# 2. Solidity 스크립트

## `contracts/script/`

스크립트는 블록체인에 배포되는 핵심 서비스 코드가 아니라, 컨트랙트를 배포하거나 특정 시나리오를 자동 실행하는 프로그램이다.

```text
src/     실제 제품 코드
script/  설치·배포·시연 자동화
test/    정답 여부 자동 검증
```

## `Deploy.s.sol`

역할:

> 컨트랙트 배포를 자동화

컨트랙트는 서로 주소로 연결되기 때문에 배포 순서가 중요하다.

수행하는 작업:

1. MockKRW 배포
2. mSEC 배포
3. PriceOracle 배포
4. ExchangeVault 배포
5. ExchangeVault를 mSEC minter로 지정
6. 배포된 주소 출력

수동으로 배포하려면 여러 명령을 실행하고 주소를 복사해야 한다. 스크립트는 이 과정을 한 번에 처리한다.

실행 예:

```powershell
forge script script/Deploy.s.sol `
  --rpc-url local `
  --broadcast `
  --private-key <배포자 개인키>
```

옵션 의미:

- `--rpc-url local`: Anvil에 연결
- `--broadcast`: 계산만 하지 않고 실제 트랜잭션 전송
- `--private-key`: 배포 트랜잭션 서명

배포 후 출력된 주소는 백엔드 환경 변수로 연결한다.

```text
MOCK_KRW_ADDRESS
MSEC_ADDRESS
PRICE_ORACLE_ADDRESS
EXCHANGE_VAULT_ADDRESS
```

## `Scenario.s.sol`

역할:

> 프로젝트의 핵심 거래 흐름을 실제 Anvil에서 한 번에 시연하고 검증

수행하는 작업:

```text
컨트랙트 배포
→ Vault에 mKRW 유동성 공급
→ 사용자에게 mKRW 지급
→ Vault approve
→ 750,000 mKRW로 mSEC 매수
→ 가격 75,000에서 80,000으로 변경
→ mSEC 전량 매도
→ 최종 잔고 확인
```

`require()`로 성공 조건을 검사하므로 문제가 생기면 스크립트가 실패한다.

`Deploy.s.sol`과의 차이:

| 파일 | 목적 |
|---|---|
| `Deploy.s.sol` | 컨트랙트만 배포 |
| `Scenario.s.sol` | 배포하고 매수·가격 변경·매도까지 실행 |

실제 백엔드 개발에는 `Deploy.s.sol`을 사용하고, 발표나 전체 흐름 확인에는 `Scenario.s.sol`이 유용하다.

---

# 3. Solidity 테스트

## `contracts/test/Exchange.t.sol`

역할:

> 컨트랙트 규칙을 자동 검증하는 Foundry 테스트

현재 20개 테스트가 있다.

검증 범위:

- faucet
- 오라클 가격
- 매수·매도
- 가격 상승에 따른 수익
- 유동성 부족
- 권한 검사
- 0 입력
- 0 주소
- 수수료 상한
- 이벤트
- fuzz 견적 계산

실행:

```powershell
forge test -vv
```

스크립트와 테스트의 차이는 다음과 같다.

| 구분 | 테스트 | 스크립트 |
|---|---|---|
| 주목적 | 코드 정합성 검증 | 배포·실행 자동화 |
| 실행 환경 | Foundry 테스트 EVM | Anvil 또는 테스트넷 |
| 결과 | 테스트 통과/실패 | 트랜잭션 실행 결과 |
| 테스트용 조작 | `deal`, `prank` 가능 | 실제 계정과 트랜잭션 사용 |
| CI 사용 | 적합 | 제한적으로 사용 |

---

# 4. Solidity 설정과 생성 디렉터리

## `foundry.toml`

Foundry 프로젝트 설정이다.

주요 설정:

```toml
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
```

- Solidity 소스 위치
- 빌드 결과 위치
- 외부 라이브러리 위치
- 컴파일러 버전

```toml
optimizer = true
optimizer_runs = 200
```

컨트랙트 바이트코드와 가스 사용량을 최적화한다.

```toml
local = "http://127.0.0.1:8545"
```

`local`이라는 이름으로 Anvil 주소를 사용할 수 있게 한다.

## `lib/`

외부 Solidity 라이브러리다.

- OpenZeppelin
- forge-std

Git에 포함하지 않고 필요할 때 설치한다.

## `out/`

컴파일 결과다.

- ABI
- bytecode
- 메타데이터

Phase 3에서 백엔드가 컨트랙트를 호출할 때 ABI 또는 여기서 생성한 Java Wrapper가 필요하다.

## `broadcast/`

배포·스크립트 실행 결과다.

- 전송한 트랜잭션
- 배포된 컨트랙트 주소
- receipt
- 체인별 실행 기록

## `cache/`

Foundry가 빌드와 스크립트 실행을 빠르게 하기 위해 사용하는 캐시다.

`out`, `broadcast`, `cache`, `lib`는 개발 과정에서 다시 생성할 수 있으므로 Git에서 제외된다.

---

# 5. 백엔드 전체 구조

```text
backend/src/main/java/com/pricetrack/exchange
├─ auth
├─ blockchain
├─ common
├─ market
├─ order
├─ portfolio
├─ quote
├─ trade
├─ user
├─ wallet
└─ websocket
```

현재 백엔드는 완성된 서비스가 아니라 Phase 2와 Phase 3를 준비한 스캐폴딩이다.

---

# 6. 백엔드 진입점

## `ExchangeApplication.java`

역할:

> Spring Boot 애플리케이션 시작점

```java
@SpringBootApplication
@EnableScheduling
@ConfigurationPropertiesScan
```

- Spring Boot 실행
- 주기적 가격 업데이트 활성화
- `application.yml` 설정값 바인딩 활성화

실행:

```powershell
.\gradlew.bat bootRun
```

---

# 7. 백엔드 `common`

## `HealthController.java`

역할:

> 서버가 정상 기동했는지 확인

API:

```http
GET /api/health
```

응답:

```json
{
  "status": "UP",
  "service": "price-tracking-exchange",
  "timestamp": "..."
}
```

이 기능은 실제로 구현되어 있다.

## `SecurityConfig.java`

역할:

> Spring Security 정책 설정

현재 설정:

- CSRF 비활성화
- 세션을 사용하지 않는 stateless 방식
- 헬스 체크·인증·마켓·WebSocket 공개
- 나머지 API도 임시로 모두 허용

현재는 스캐폴딩이므로 다음 상태다.

```java
.anyRequest().permitAll()
```

Phase 2.1에서는 다음과 같이 변경해야 한다.

```text
회원가입·로그인·마켓 조회 → 공개
주문·포트폴리오·내 정보 → JWT 인증 필요
```

---

# 8. 백엔드 `auth`

## `JwtTokenProvider.java`

역할:

> JWT Access Token 생성과 검증

구현된 기능:

### `createToken(userId, email)`

JWT에 다음 정보를 넣는다.

- 사용자 ID
- 이메일
- 발급 시각
- 만료 시각
- 서명

### `getUserId(token)`

JWT를 검증하고 사용자 ID를 추출한다.

현재 미구현:

- HTTP 요청에서 Authorization 헤더 읽기
- JWT 인증 필터
- SecurityContext에 사용자 등록
- 만료·위조 토큰 예외 응답

## `AuthController.java`

역할:

> 회원가입·로그인·내 정보 API 정의

API 경로는 만들어져 있다.

```http
POST /api/auth/signup
POST /api/auth/login
GET  /api/me
```

하지만 전부 `UnsupportedOperationException`을 발생시킨다. API 모양만 있고 실제 회원가입·로그인은 아직 없다.

---

# 9. 백엔드 `user`

## `User.java`

역할:

> `users` 테이블과 연결되는 JPA 엔티티

저장 필드:

```text
id
email
passwordHash
nickname
walletAddress
createdAt
```

회원가입이 구현되면 이 객체로 사용자를 DB에 저장한다. 비밀번호 원문이 아니라 BCrypt 해시를 `passwordHash`에 저장해야 한다.

## `UserRepository.java`

역할:

> 사용자 DB 조회

기능:

```java
findByEmail(email)
existsByEmail(email)
```

Spring Data JPA가 메서드 이름을 해석해 SQL을 자동 생성한다.

---

# 10. 백엔드 `market`

## `PriceSimulator.java`

역할:

> 실제 삼성전자 API 대신 모의 가격을 생성

초기 가격은 75,000원이며, 기본 1초마다 -0.3%에서 +0.3% 범위에서 가격을 무작위로 변경한다.

변경한 가격은 다음 WebSocket 토픽으로 전송한다.

```text
/topic/markets/mSEC/price
```

현재 동작:

- 메모리에 현재 가격 저장
- 랜덤 가격 생성
- WebSocket 가격 이벤트 발행

미구현:

- `price_ticks` 테이블 저장
- PriceOracle 온체인 업데이트
- 서버 재시작 후 가격 복원

## `MarketController.java`

역할:

> 현재 마켓 가격 조회 API

구현된 API:

```http
GET /api/markets
GET /api/markets/{symbol}
```

현재는 mSEC 하나만 반환한다.

미구현 API:

```http
GET /api/markets/{symbol}/ticks
```

가격 이력 DB 조회가 없어서 예외가 발생한다.

---

# 11. 백엔드 `quote`

## `QuoteController.java`

역할:

> 매수·매도 전 예상 결과 계산

구현된 API:

```http
POST /api/quotes/buy
POST /api/quotes/sell
```

매수 견적:

```text
입력 mKRW
→ 수수료 0.1% 계산
→ 현재 가격으로 예상 mSEC 계산
```

매도 견적:

```text
입력 mSEC
→ 현재 가격으로 총액 계산
→ 수수료 0.1% 차감
→ 예상 mKRW 계산
```

이 기능은 실제 동작한다.

다만 현재 계산은 Controller 안에 있다. Phase 2에서는 별도 `QuoteService`로 분리하는 것이 좋다.

```text
Controller → HTTP 입출력
Service    → 금융 계산 규칙
```

그래야 단위 테스트하기 쉽고 주문 처리에서도 같은 계산을 재사용할 수 있다.

---

# 12. 백엔드 `order`

## `OrderSide.java`

주문 방향이다.

```text
BUY
SELL
```

## `OrderStatus.java`

온체인 비동기 거래 상태다.

```text
REQUESTED
PENDING_ONCHAIN
FILLED
FAILED
CANCELED
```

상태 흐름:

```text
REQUESTED
→ PENDING_ONCHAIN
→ FILLED 또는 FAILED
```

## `OrderController.java`

역할:

> 주문 생성과 조회 API

경로는 정의되어 있다.

```http
POST /api/orders/buy
POST /api/orders/sell
GET  /api/orders
GET  /api/orders/{orderId}
```

하지만 실제 주문 저장·체결 로직은 모두 미구현이다.

Phase 2에서는 DB 기반 mock 주문을 먼저 만들고, Phase 3에서 컨트랙트 호출로 교체할 예정이다.

---

# 13. 백엔드 `portfolio`

## `PortfolioController.java`

역할:

> 사용자 포트폴리오와 자산 이력 조회

API:

```http
GET /api/portfolio
GET /api/portfolio/history
```

현재는 전부 미구현이다.

나중에 다음 정보를 계산한다.

```text
mKRW 잔고
mSEC 잔고
mSEC 현재 평가금액
평균 매수가
평가손익
수익률
총 평가금액
```

---

# 14. 백엔드 `trade`

## `TradeService.java`

역할:

> 체결 내역 저장과 체결 이벤트 처리

현재는 빈 클래스에 가까운 스텁이다.

Phase 3에서 다음 역할을 맡게 된다.

```text
Bought/Sold 이벤트 파싱
→ trades 테이블 저장
→ 주문 FILLED 처리
→ WebSocket 체결 이벤트 발행
```

---

# 15. 백엔드 `wallet`

## `WalletService.java`

역할:

> 사용자 지갑 주소 생성 또는 연결

예정된 기능:

```java
assignWallet(userId)
```

현재는 미구현이다.

이 클래스 구현 전에 지갑 정책을 먼저 결정해야 한다.

- 사용자가 직접 개인키 관리
- 백엔드가 사용자별 개인키 관리
- 하나의 운영 지갑과 DB 잔고 사용

이 결정은 Phase 3의 컨트랙트 호출 방식에 직접 영향을 준다.

---

# 16. 백엔드 `blockchain`

## `BlockchainProperties.java`

역할:

> `application.yml`의 블록체인 설정을 Java 객체로 변환

설정값:

```text
rpcUrl
mockKrwAddress
mSecAddress
priceOracleAddress
exchangeVaultAddress
operatorPrivateKey
```

배포 스크립트가 출력한 주소를 여기로 연결한다.

## `BlockchainService.java`

역할:

> web3j를 사용해 Ethereum 노드와 통신

현재 구현된 기능:

```java
latestBlockNumber()
```

Anvil 또는 Ethereum 노드의 최신 블록 번호를 조회한다.

아직 미구현:

- mKRW faucet
- approve
- Vault buy/sell
- 오라클 가격 변경
- receipt polling
- 이벤트 파싱
- 실패 트랜잭션 처리
- 주문 상태 업데이트

즉 현재는 RPC 연결 확인 수준이다.

---

# 17. 백엔드 `websocket`

## `WebSocketConfig.java`

역할:

> 실시간 이벤트 전송을 위한 STOMP WebSocket 설정

접속 엔드포인트:

```text
/ws
```

공개 토픽:

```text
/topic/markets/mSEC/price
/topic/markets/mSEC/trades
```

사용자별 큐:

```text
/user/queue/orders
/user/queue/portfolio
```

현재 가격 이벤트는 전송하지만 다음은 아직 없다.

- 체결 이벤트
- 사용자별 주문 상태
- 사용자별 포트폴리오
- WebSocket JWT 인증

---

# 18. 백엔드 리소스

## `application.yml`

역할:

> 서버 환경 설정

주요 설정:

- PostgreSQL 주소와 계정
- 서버 포트
- JWT 비밀키와 만료 시간
- 가격 업데이트 주기
- Ethereum RPC 주소
- 컨트랙트 주소
- 운영자 개인키

환경 변수를 사용하면 코드 수정 없이 값을 변경할 수 있다.

```text
DB_URL
JWT_SECRET
RPC_URL
MOCK_KRW_ADDRESS
MSEC_ADDRESS
PRICE_ORACLE_ADDRESS
EXCHANGE_VAULT_ADDRESS
OPERATOR_PRIVATE_KEY
```

실제 개인키는 파일에 직접 작성하면 안 된다.

## `schema.sql`

역할:

> PostgreSQL 테이블 생성

테이블:

```text
users
assets
price_ticks
orders
trades
blockchain_transactions
```

초기 자산:

```text
mKRW
mSEC
```

현재는 `users`만 JPA 엔티티가 있으며 나머지 엔티티와 Repository는 아직 구현되지 않았다.

## 테스트용 `application.yml`

경로:

```text
backend/src/test/resources/application.yml
```

백엔드 테스트에서는 실제 PostgreSQL 대신 H2 인메모리 DB를 사용한다.

현재 백엔드 테스트는 Spring Context 기동 확인 하나뿐이다.

---

# 19. 백엔드 빌드 파일

## `build.gradle`

역할:

> Java 버전, 라이브러리, 테스트 환경 정의

주요 의존성:

- Spring Web
- Spring Security
- Spring Data JPA
- Spring WebSocket
- PostgreSQL
- web3j
- JWT
- Lombok
- H2 테스트 DB

## `gradlew`, `gradlew.bat`

Gradle Wrapper 실행 파일이다.

PC에 Gradle을 따로 설치하지 않아도 프로젝트에서 정한 Gradle 버전을 사용한다.

Windows:

```powershell
.\gradlew.bat test
.\gradlew.bat bootRun
```

Linux/macOS:

```bash
./gradlew test
./gradlew bootRun
```

## `gradle/wrapper/gradle-wrapper.jar`

Gradle Wrapper 내부 실행 코드다. 직접 수정하지 않는다.

---

# 20. 현재 구현 상태 요약

| 영역 | 실제 동작 | 미구현 |
|---|---|---|
| mKRW | ERC-20, faucet | 사용 제한 정책 |
| mSEC | mint/burn, minter | 고급 권한 정책 |
| Oracle | 가격 저장·변경 | stale price 검사 |
| Vault | 견적·매수·매도 | 슬리피지 보호 |
| 배포 | 자동 배포 | 테스트넷 검증 |
| 컨트랙트 테스트 | 20개 통과 | invariant 테스트 |
| 백엔드 인증 | JWT 생성 일부 | 회원가입·로그인·필터 |
| 현재가 | 조회·시뮬레이션 | DB 저장·온체인 반영 |
| 견적 | 실제 계산 | 서비스 계층·입력 검증 |
| 주문 | API 형태만 존재 | 저장·체결 전체 |
| 포트폴리오 | API 형태만 존재 | 계산 전체 |
| 블록체인 연결 | 블록 번호 조회 | 거래·receipt·이벤트 |
| WebSocket | 가격 발행 | 주문·체결·포트폴리오 |

핵심만 압축하면 다음과 같다.

```text
Solidity
→ 온체인 거래 기능은 구현 완료

Spring Boot
→ 가격·견적·WebSocket 골격만 동작
→ 인증·주문·체결·포트폴리오·온체인 연동은 앞으로 구현
```

스크립트는 다음처럼 기억하면 된다.

```text
Deploy.s.sol
= 컨트랙트 설치 프로그램

Scenario.s.sol
= 전체 거래 시연 프로그램

Exchange.t.sol
= 컨트랙트 자동 시험지
```

앞으로 Phase 2에서는 Solidity 코드를 크게 늘리기보다 백엔드가 현재 컨트랙트를 사용할 수 있도록 인증, DB, 주문, 체결 구조를 구현하는 것이 중심이다.
